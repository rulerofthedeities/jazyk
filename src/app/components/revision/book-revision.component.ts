import { Component } from '@angular/core';

@Component({
  templateUrl: 'book-revision.component.html',
  styleUrls: ['book-revision.component.css']
})

export class BookRevisionComponent {
  // First check if book is read at least once
}
