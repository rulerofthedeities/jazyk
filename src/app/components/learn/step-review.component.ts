import {Component} from '@angular/core';
import {Step} from './step-base.component';

@Component({
  selector: 'km-learn-review',
  template: `REVIEW`
})

export class LearnReviewComponent extends Step {
  
}
